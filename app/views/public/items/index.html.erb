<h1>商品一覧</h1>


<% @items.each do |item| %>
<table>
  <tr>
    <td>
    <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image %>
    <% end %>

    <%= item.name %>
    <td><%= item.with_tax_price%></td>円
    </td>

  </tr>
</table>

<% end %>
